---
phase: 05-data-integrity-tracking-segments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routes/tracking.ts
  - packages/api/tests/tracking.test.ts
autonomous: true
requirements: [DATA-08, DATA-09]
must_haves:
  truths:
    - "Click tracking redirect resolves the original URL from the SENT event link_map, not from a CLICK event scan"
    - "A tracked link with UTM parameters or encoded query strings reaches the original URL unchanged"
    - "The CLICK event query path in the redirect handler is removed entirely"
  artifacts:
    - path: "packages/api/src/routes/tracking.ts"
      provides: "Fixed click redirect handler"
      contains: "event_type.*SENT"
    - path: "packages/api/tests/tracking.test.ts"
      provides: "Tests for redirect URL preservation and lookup order"
  key_links:
    - from: "packages/api/src/routes/tracking.ts"
      to: "events table (SENT event)"
      via: "Kysely query filtering event_type = SENT"
      pattern: "EventType\\.SENT"
---

<objective>
Fix click tracking redirect to query SENT event link_map first (DATA-09) and confirm URL preservation (DATA-08).

Purpose: The current redirect handler queries CLICK events first (slow JSONB scan on partitioned table, wrong source of truth). The SENT event link_map is the canonical URL source written atomically at send time. Fixing the lookup order also resolves DATA-08 because the link_map stores URLs verbatim.

Output: Updated tracking.ts with SENT-event-first lookup (CLICK event query removed), plus unit tests confirming URL preservation with UTM params and encoded characters.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-integrity-tracking-segments/05-RESEARCH.md

<interfaces>
<!-- Current tracking.ts click redirect handler (lines 49-101) uses CLICK event first, SENT fallback -->
<!-- After fix: SENT event link_map is the only lookup path -->

From packages/shared/src/types.ts:
```typescript
export const EventType = {
  SENT: 1,
  OPEN: 2,
  CLICK: 3,
  BOUNCE: 4,
  COMPLAINT: 5,
  UNSUBSCRIBE: 6,
  MACHINE_OPEN: 7,
} as const;
```

From packages/workers/src/tracking.ts (rewriteLinks - already correct):
```typescript
// rewriteLinks stores original URL verbatim in linkMap
// linkMap is { [sha256hash]: originalUrl }
// Written to SENT event metadata as { link_map: linkMap }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix click redirect to use SENT event link_map (DATA-08, DATA-09)</name>
  <files>packages/api/src/routes/tracking.ts, packages/api/tests/tracking.test.ts</files>
  <behavior>
    - Test 1: Redirect resolves URL from SENT event link_map when messageId and linkHash match
    - Test 2: Redirect preserves URL with UTM parameters (e.g., ?utm_source=email&utm_medium=campaign)
    - Test 3: Redirect preserves URL with encoded query strings (e.g., %20, %3D, %26)
    - Test 4: Redirect returns fallback URL when messageId has no SENT event
    - Test 5: Redirect returns fallback URL when linkHash not found in link_map
    - Test 6: Redirect blocks non-http/https protocols (javascript:, data:)
  </behavior>
  <action>
    1. In packages/api/src/routes/tracking.ts, replace the click redirect handler (lines 57-85) to remove the CLICK event query entirely. The new handler should:
       - Query events table for event_type = EventType.SENT where message_id = messageId (uses idx_events_message index)
       - Extract link_map from sentEvent.metadata
       - Look up linkHash in link_map to get targetUrl
       - Keep the existing URL protocol validation (lines 87-95 unchanged)
       - Keep the existing recordClick fire-and-forget call (line 98 unchanged)
       - Keep the existing reply.redirect(targetUrl) — use raw string, NOT url.href (avoids double-encoding)

    2. The fixed handler should look like the code example in 05-RESEARCH.md "DATA-09 Fixed Redirect Handler" section.

    3. Add tests in packages/api/tests/tracking.test.ts that test the redirect URL resolution logic. Since the tracking route handler uses getDb() directly (no dependency injection), test the redirect behavior by:
       - If Vitest test setup allows route testing (check existing test patterns), test via app.inject()
       - Otherwise, extract the URL resolution logic into a testable helper function and test that

    4. Do NOT modify recordClick — it correctly stores url and link_hash in CLICK event metadata for audit purposes. The CLICK event is write-only audit data, not a lookup source.
  </action>
  <verify>
    <automated>cd packages/api && npx vitest run tests/tracking.test.ts</automated>
  </verify>
  <done>
    - CLICK event query removed from redirect handler
    - SENT event link_map is the sole URL resolution path
    - Tests pass for UTM params, encoded strings, missing messageId, invalid protocols
    - recordClick still fires (audit trail preserved)
  </done>
</task>

</tasks>

<verification>
- `cd packages/api && npx vitest run tests/tracking.test.ts` passes
- `grep -q "EventType.CLICK" packages/api/src/routes/tracking.ts` should NOT match in the redirect handler (CLICK query removed)
- `grep -q "EventType.SENT" packages/api/src/routes/tracking.ts` should match in the redirect handler
</verification>

<success_criteria>
1. Click redirect handler queries SENT event link_map as sole URL source (DATA-09)
2. URLs with UTM parameters and encoded query strings pass through unchanged (DATA-08)
3. All new tests pass
4. No regression in existing tracking tests (detectMachineOpen, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-integrity-tracking-segments/05-01-SUMMARY.md`
</output>
