---
phase: 04-data-integrity-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routes/tracking.ts
  - packages/api/src/services/segments.service.ts
  - packages/api/src/plugins/auth.ts
autonomous: true
requirements:
  - DATA-06

must_haves:
  truths:
    - "Every fire-and-forget .catch() logs the error with contextual identifiers"
    - "No .catch(() => {}) (empty callback) remains in the codebase"
    - "Fire-and-forget async model is preserved — no new await added"
  artifacts:
    - path: "packages/api/src/routes/tracking.ts"
      provides: "Error logging for recordOpen and recordClick"
      contains: "request.log.error"
    - path: "packages/api/src/services/segments.service.ts"
      provides: "Error logging for segment cache writes"
      contains: "console.warn"
    - path: "packages/api/src/plugins/auth.ts"
      provides: "Error logging for last_used_at update"
      contains: "console.warn"
  key_links:
    - from: "packages/api/src/routes/tracking.ts"
      to: "Pino request logger"
      via: "request.log.error({ err, messageId }, msg)"
      pattern: "request\\.log\\.error"
---

<objective>
Replace all five silent `.catch(() => {})` callbacks with proper error logging that includes contextual identifiers (campaignId, contactId, messageId, segmentId, etc.).

Purpose: DATA-06 requires that no error in the send pipeline is silently swallowed. Currently five sites discard errors entirely, making production debugging impossible.
Output: Three modified files with contextual error logging in every catch callback.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-integrity-error-handling/04-RESEARCH.md

@packages/api/src/routes/tracking.ts
@packages/api/src/services/segments.service.ts
@packages/api/src/plugins/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error logging to tracking route catch callbacks</name>
  <files>packages/api/src/routes/tracking.ts</files>
  <action>
Replace the two silent `.catch(() => {})` callbacks in tracking.ts with contextual error logging using Fastify's `request.log`:

1. Line 43 — `recordOpen(db, messageId, request).catch(() => {})`:
   Replace with `.catch((err) => { request.log.error({ err, messageId }, 'recordOpen failed'); })`

2. Line 98 — `recordClick(db, messageId, linkHash, targetUrl, request).catch(() => {})`:
   Replace with `.catch((err) => { request.log.error({ err, messageId, linkHash }, 'recordClick failed'); })`

IMPORTANT: Do NOT add `await` — these must remain fire-and-forget. The pixel/redirect response must return immediately without waiting for the DB write. Only change the catch callback contents.
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && grep -n "\.catch(() => {})" packages/api/src/routes/tracking.ts | wc -l | tr -d ' '</automated>
  </verify>
  <done>Both tracking route catch callbacks log errors with messageId context. grep for `.catch(() => {})` in tracking.ts returns 0 matches. Fire-and-forget model unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add error logging to segments service and auth plugin catch callbacks</name>
  <files>packages/api/src/services/segments.service.ts, packages/api/src/plugins/auth.ts</files>
  <action>
Replace the three remaining silent `.catch(() => {})` callbacks:

1. segments.service.ts line 143 — cache write after getSegmentContacts():
   Replace with `.catch((err) => { console.warn('Failed to update segment contacts cache', { err, segmentId }); })`

2. segments.service.ts line 216 — cache write after getSegmentCount():
   Replace with `.catch((err) => { console.warn('Failed to update segment count cache', { err, segmentId }); })`

   For both segment sites: use `console.warn` (not `console.error`) because cache write failure is non-critical — stale count is acceptable. Include `segmentId` in the log context. The variable name for the segment ID may be `segmentId`, `id`, or derived from a parameter — read the surrounding function to identify the correct variable name.

3. auth.ts line 136 — last_used_at update for API key:
   Replace with `.catch((err) => { console.warn('Failed to update API key last_used_at', { err }); })`

   Use `console.warn` — this is housekeeping, not critical. Do NOT log the full API key (security). If a key prefix or ID is available in scope, include it.

IMPORTANT: Do NOT add `await` to any of these. They must remain fire-and-forget.
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && grep -rn "\.catch(() => {})" packages/api/src/services/segments.service.ts packages/api/src/plugins/auth.ts | wc -l | tr -d ' '</automated>
  </verify>
  <done>All three catch callbacks log errors with context. grep for `.catch(() => {})` in segments.service.ts and auth.ts returns 0 matches. No `await` added. Cache failures log at warn level, not error.</done>
</task>

</tasks>

<verification>
Run a codebase-wide check that no silent `.catch(() => {})` remains:
```bash
grep -rn '\.catch(() => {})' packages/
```
Expected: 0 matches.

Verify no `await` was accidentally added to fire-and-forget calls:
```bash
grep -n 'await recordOpen\|await recordClick' packages/api/src/routes/tracking.ts
```
Expected: 0 matches.
</verification>

<success_criteria>
- Zero `.catch(() => {})` patterns in the codebase
- All five catch callbacks log with contextual identifiers (messageId, segmentId, etc.)
- Fire-and-forget async model preserved (no await added)
- Tracking routes use `request.log.error`, services/plugins use `console.warn`
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-integrity-error-handling/04-01-SUMMARY.md`
</output>
