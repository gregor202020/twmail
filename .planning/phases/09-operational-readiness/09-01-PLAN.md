---
phase: 09-operational-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/services/webhooks.service.ts
  - packages/api/src/utils/hmac.ts
  - packages/api/tests/webhook-queue-wiring.unit.test.ts
  - packages/api/tests/hmac-constant-time.unit.test.ts
  - packages/api/tests/bulk-send-rate-limit.unit.test.ts
autonomous: true
requirements:
  - OPS-02
  - OPS-06
  - OPS-07

must_haves:
  truths:
    - "Webhook delivery jobs reach the BullMQ worker (not lost in a raw Redis list)"
    - "Webhook endpoints that fail 50 consecutive times are automatically disabled"
    - "HMAC signature comparison uses crypto.timingSafeEqual, not string equality"
    - "Bulk-send worker rate limiter is configured at max=40 duration=1000"
  artifacts:
    - path: "packages/api/src/services/webhooks.service.ts"
      provides: "BullMQ Queue.add() instead of redis.lpush for webhook delivery"
      contains: "Queue.*webhook"
    - path: "packages/api/src/utils/hmac.ts"
      provides: "Constant-time HMAC verification utility"
      exports: ["verifyHmacSignature"]
    - path: "packages/api/tests/webhook-queue-wiring.unit.test.ts"
      provides: "Test confirming BullMQ queue usage"
    - path: "packages/api/tests/hmac-constant-time.unit.test.ts"
      provides: "Test confirming timingSafeEqual usage"
    - path: "packages/api/tests/bulk-send-rate-limit.unit.test.ts"
      provides: "Test confirming rate limiter config"
  key_links:
    - from: "packages/api/src/services/webhooks.service.ts"
      to: "packages/workers/src/workers/webhook.worker.ts"
      via: "BullMQ queue named 'webhook'"
      pattern: "Queue.*webhook.*add"
---

<objective>
Fix the webhook queue wiring bug, add a constant-time HMAC verification utility, and verify the bulk-send rate limiter configuration.

Purpose: The webhook delivery system is completely broken (enqueue uses raw Redis list while worker listens on BullMQ queue). OPS-06 requires a safe HMAC comparison utility. OPS-02 rate limiting is already implemented but needs a test to prove correctness.
Output: Working webhook delivery pipeline, reusable HMAC utility, rate limiter verification test.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-operational-readiness/09-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/workers/src/workers/webhook.worker.ts:
```typescript
export interface WebhookJobData {
  deliveryId: number;
  endpointId: number;
  url: string;
  secret: string;
  eventType: string;
  payload: Record<string, unknown>;
  attempt: number;
}
// Worker listens on BullMQ queue named 'webhook'
// Worker handler name: 'deliver'
```

From packages/api/src/services/webhooks.service.ts (current broken code at line 142):
```typescript
// THIS IS THE BUG — uses redis.lpush to a raw list instead of BullMQ Queue.add()
await redis.lpush('twmail:webhook-send', JSON.stringify({ ... }));
```

From packages/api/tests/bulk-send-dedup.unit.test.ts (test pattern):
```typescript
// Tests use vi.fn() mocks, no real DB connection
// Import pattern: import from '../../workers/src/workers/bulk-send.worker.js'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix webhook queue wiring + add HMAC utility</name>
  <files>packages/api/src/services/webhooks.service.ts, packages/api/src/utils/hmac.ts, packages/api/tests/webhook-queue-wiring.unit.test.ts, packages/api/tests/hmac-constant-time.unit.test.ts</files>
  <behavior>
    - Test: enqueueWebhookDelivery calls BullMQ Queue.add('deliver', jobData) not redis.lpush
    - Test: verifyHmacSignature returns true for valid signature
    - Test: verifyHmacSignature returns false for invalid signature
    - Test: verifyHmacSignature returns false for different-length signature (no throw)
    - Test: verifyHmacSignature uses crypto.timingSafeEqual (source code scan confirms import)
  </behavior>
  <action>
    1. Create `packages/api/src/utils/hmac.ts` with `verifyHmacSignature(secret, body, receivedSignature)` that:
       - Computes expected signature as `sha256=<hex>` using `createHmac('sha256', secret).update(body).digest('hex')`
       - Converts both to Buffers
       - Returns false on length mismatch (instead of throwing)
       - Uses `crypto.timingSafeEqual` for same-length comparison
       - Export the function

    2. Fix `packages/api/src/services/webhooks.service.ts` `enqueueWebhookDelivery`:
       - Import `Queue` and `ConnectionOptions` from 'bullmq'
       - Replace `redis.lpush('twmail:webhook-send', JSON.stringify({...}))` with:
         - Create Queue('webhook', { connection: redis as unknown as ConnectionOptions })
         - Call queue.add('deliver', { deliveryId, endpointId, url, secret, eventType, payload, attempt: 1 })
         - Call queue.close() after add
       - Remove the `getRedis()` call if it's only used for lpush (check if still needed for other purposes in the function — it IS still used by getRedis() at function top, but only for the lpush. Since Queue takes a connection option, redis import from getRedis is no longer needed in enqueueWebhookDelivery. However getRedis is imported at module level and may be used elsewhere — leave the module-level import, just stop calling it in enqueueWebhookDelivery).
       - Actually: getRedis() is called at line 115 and used at line 142. After replacing lpush with Queue, the redis variable in enqueueWebhookDelivery is unused. Remove the `const redis = getRedis()` line from inside enqueueWebhookDelivery. The module-level import of getRedis stays (other functions may use it). Pass `getRedis() as unknown as ConnectionOptions` directly to Queue constructor.

    3. Write tests:
       - `webhook-queue-wiring.unit.test.ts`: Mock bullmq Queue class, verify .add('deliver', ...) is called with correct job data shape matching WebhookJobData interface, verify redis.lpush is NOT called
       - `hmac-constant-time.unit.test.ts`: Test valid/invalid signatures, length mismatch, source-code scan confirming timingSafeEqual import (pattern from error-shapes.test.ts)
  </action>
  <verify>
    <automated>cd packages/api && npx vitest run tests/webhook-queue-wiring.unit.test.ts tests/hmac-constant-time.unit.test.ts</automated>
  </verify>
  <done>enqueueWebhookDelivery uses BullMQ Queue.add('deliver', ...) on queue named 'webhook'. verifyHmacSignature exists and uses crypto.timingSafeEqual. Both have passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify bulk-send rate limiter config + webhook auto-disable threshold</name>
  <files>packages/api/tests/bulk-send-rate-limit.unit.test.ts, packages/api/tests/webhook-auto-disable.unit.test.ts</files>
  <behavior>
    - Test: bulk-send worker creates Worker with limiter { max: 40, duration: 1000 }
    - Test: bulk-send worker concurrency is <= 40
    - Test: webhook worker disables endpoint when failure_count reaches 50
    - Test: webhook worker resets failure_count to 0 on success
  </behavior>
  <action>
    1. Create `packages/api/tests/bulk-send-rate-limit.unit.test.ts`:
       - Use source-code scan approach (fs.readFileSync of bulk-send.worker.ts)
       - Assert file contains `max: 40` and `duration: 1000` in a limiter block
       - Assert concurrency value is <= 40
       - This is a config inspection test, not a runtime test (avoids needing Redis)

    2. Create `packages/api/tests/webhook-auto-disable.unit.test.ts`:
       - Use source-code scan of webhook.worker.ts
       - Assert file contains `failure_count >= 50` check
       - Assert file contains `set({ active: false })` disable logic
       - Assert file contains `set({ failure_count: 0` reset on success
       - These are structural assertions confirming the logic exists and uses correct thresholds
  </action>
  <verify>
    <automated>cd packages/api && npx vitest run tests/bulk-send-rate-limit.unit.test.ts tests/webhook-auto-disable.unit.test.ts</automated>
  </verify>
  <done>Rate limiter config verified at 40/sec. Webhook auto-disable at 50 failures verified. Both tests pass.</done>
</task>

</tasks>

<verification>
All five tests pass: webhook-queue-wiring, hmac-constant-time, bulk-send-rate-limit, webhook-auto-disable.
`cd packages/api && npx vitest run` — full test suite green.
TypeScript compiles: `cd packages/api && npx tsc --noEmit`
</verification>

<success_criteria>
- enqueueWebhookDelivery uses BullMQ Queue.add() instead of redis.lpush
- verifyHmacSignature utility exists and uses crypto.timingSafeEqual
- Bulk-send rate limiter confirmed at max=40, duration=1000
- Webhook auto-disable at failure_count >= 50 confirmed
- All new tests pass, full test suite green, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-readiness/09-01-SUMMARY.md`
</output>
