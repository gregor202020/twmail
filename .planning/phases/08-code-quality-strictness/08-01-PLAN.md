---
phase: 08-code-quality-strictness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schema.ts
  - packages/shared/src/segments.ts
  - packages/api/src/middleware/auth.ts
  - packages/api/src/plugins/error-handler.ts
  - packages/api/src/routes/assets.ts
  - packages/api/src/routes/users.ts
  - packages/api/src/routes/webhooks-inbound.ts
  - packages/api/src/services/auth.service.ts
  - packages/api/src/services/contacts.service.ts
  - packages/api/src/services/segments.service.ts
  - packages/api/src/services/settings.service.ts
  - packages/workers/src/workers/bulk-send.worker.ts
  - packages/workers/src/workers/import.worker.ts
  - packages/workers/src/workers/ab-eval.worker.ts
  - packages/workers/src/workers/resend.worker.ts
  - packages/workers/src/workers/webhook.worker.ts
  - packages/workers/src/scheduler.ts
  - eslint.config.mjs
autonomous: true
requirements: [QUAL-08]

must_haves:
  truths:
    - "tsc --noEmit exits 0 across all three packages (api, workers, shared)"
    - "ESLint exits 0 with no-explicit-any and all unsafe-* rules at error level"
    - "No as any cast exists in packages/api/src, packages/workers/src, or packages/shared/src"
  artifacts:
    - path: "packages/shared/src/schema.ts"
      provides: "CampaignHoldbackContactsTable type + Database interface entry"
      contains: "campaign_holdback_contacts"
    - path: "eslint.config.mjs"
      provides: "Upgraded ESLint rules from warn to error"
      contains: "'error'"
  key_links:
    - from: "packages/workers/src/workers/bulk-send.worker.ts"
      to: "packages/shared/src/schema.ts"
      via: "Database interface includes campaign_holdback_contacts"
      pattern: "campaign_holdback_contacts"
    - from: "eslint.config.mjs"
      to: "all source files"
      via: "no-explicit-any and unsafe-* at error level"
      pattern: "no-explicit-any.*error"
---

<objective>
Remove every `as any` cast from service and worker code, fix pre-existing compile errors, and upgrade ESLint strict-type rules from warn to error.

Purpose: QUAL-08 requires that all packages compile under strict: true with zero suppressed type errors. The tsconfig already has strict: true, but ~25 `as any` casts bypass the checks. This plan removes them all and upgrades ESLint enforcement so they cannot return.

Output: Zero `as any` in backend source, tsc --noEmit clean across all packages, ESLint unsafe rules at error level.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-code-quality-strictness/08-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs -->

From eslint.config.mjs (lines 26-40) — rules to upgrade:
```javascript
'@typescript-eslint/no-explicit-any': 'warn',
'@typescript-eslint/no-unsafe-assignment': 'warn',
'@typescript-eslint/no-unsafe-call': 'warn',
'@typescript-eslint/no-unsafe-member-access': 'warn',
'@typescript-eslint/no-unsafe-argument': 'warn',
'@typescript-eslint/no-unsafe-return': 'warn',
```

From packages/shared/src/schema.ts — Database interface needs campaign_holdback_contacts:
```typescript
export interface Database {
  // ... existing tables
  // MISSING: campaign_holdback_contacts
}
```

Established BullMQ connection pattern (Phase 6):
```typescript
// Correct: redis as unknown as ConnectionOptions
// Wrong: redis as any
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all as-any casts and fix compile errors</name>
  <files>
    packages/shared/src/schema.ts,
    packages/shared/src/segments.ts,
    packages/api/src/middleware/auth.ts,
    packages/api/src/plugins/error-handler.ts,
    packages/api/src/routes/assets.ts,
    packages/api/src/routes/users.ts,
    packages/api/src/routes/webhooks-inbound.ts,
    packages/api/src/services/auth.service.ts,
    packages/api/src/services/contacts.service.ts,
    packages/api/src/services/segments.service.ts,
    packages/api/src/services/settings.service.ts,
    packages/workers/src/workers/bulk-send.worker.ts,
    packages/workers/src/workers/import.worker.ts,
    packages/workers/src/workers/ab-eval.worker.ts,
    packages/workers/src/workers/resend.worker.ts,
    packages/workers/src/workers/webhook.worker.ts,
    packages/workers/src/scheduler.ts
  </files>
  <action>
    Remove every `as any` cast from backend source files. The complete inventory and fix strategy is in 08-RESEARCH.md "as any Inventory" section. Apply these fixes:

    **packages/shared/src/schema.ts:**
    - Add CampaignHoldbackContactsTable interface: { id: Generated<number>, campaign_id: number, contact_id: number, created_at: Generated<Date> }
    - Add `campaign_holdback_contacts: CampaignHoldbackContactsTable` to the Database interface
    - This fixes 3 compile errors in workers package

    **packages/shared/src/segments.ts:**
    - Replace `value as any` in Kysely operators with proper Kysely typing (sql.lit or ValueExpression)

    **packages/api/src/middleware/auth.ts:**
    - Replace `(request.server as any).authenticate` with declaration merge: add `declare module 'fastify' { interface FastifyInstance { authenticate: ... } }` in the auth plugin file, then remove the cast

    **packages/api/src/plugins/error-handler.ts:**
    - Replace `error as any` with `error as FastifyError` (import FastifyError from 'fastify')

    **packages/api/src/routes/assets.ts:**
    - Replace `(request.query as any)?.campaign_id` with a Querystring generic on the route handler

    **packages/api/src/routes/users.ts:**
    - Replace `r as any` in z.refine with z.nativeEnum(UserRole) or a numeric cast

    **packages/api/src/routes/webhooks-inbound.ts:**
    - Replace `db as any` with properly typed db parameter
    - Replace result casts with Kysely InsertResult types
    - Replace `data as any`, `bounce as any` with runtime type narrowing or typed interfaces

    **packages/api/src/services/auth.service.ts:**
    - Replace `payload as any` with a JwtPayload interface

    **packages/api/src/services/contacts.service.ts:**
    - Replace `sortBy as any` with validated keyof Contact

    **packages/api/src/services/segments.service.ts:**
    - Replace `value as any` in Kysely operators with proper Kysely typing

    **packages/api/src/services/settings.service.ts:**
    - Replace `{ id: 1 } as any` with correct Kysely insert type

    **packages/workers — all redis as any:**
    - In bulk-send.worker.ts, import.worker.ts, ab-eval.worker.ts, resend.worker.ts, webhook.worker.ts, scheduler.ts: replace `redis as any` with `redis as unknown as ConnectionOptions` (established Phase 6 pattern)

    **packages/workers/src/workers/bulk-send.worker.ts:**
    - Replace `db as any` in shouldSkipSend — remove cast, db is already typed
    - Replace `(eb: any)` in updateTable callback with ExpressionBuilder<Database, 'campaigns'>

    **packages/workers/src/workers/import.worker.ts:**
    - Replace `errors as any` with proper typing

    IMPORTANT: Do NOT upgrade ESLint rules yet — that happens in Task 2 after all casts are removed (see Pitfall 3 in RESEARCH.md).

    After all changes, verify: `npx tsc --noEmit -p packages/shared/tsconfig.json && npx tsc --noEmit -p packages/api/tsconfig.json && npx tsc --noEmit -p packages/workers/tsconfig.json`
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && npx tsc --noEmit -p packages/shared/tsconfig.json && npx tsc --noEmit -p packages/api/tsconfig.json && npx tsc --noEmit -p packages/workers/tsconfig.json</automated>
  </verify>
  <done>tsc --noEmit exits 0 for all three packages. Zero `as any` casts remain in packages/api/src, packages/workers/src, packages/shared/src (verified by grep).</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ESLint strict-type rules from warn to error</name>
  <files>eslint.config.mjs</files>
  <action>
    In eslint.config.mjs, change these rules from 'warn' to 'error':
    - @typescript-eslint/no-explicit-any
    - @typescript-eslint/no-unsafe-assignment
    - @typescript-eslint/no-unsafe-call
    - @typescript-eslint/no-unsafe-member-access
    - @typescript-eslint/no-unsafe-argument
    - @typescript-eslint/no-unsafe-return

    Update the comment above these rules from "Phase 8 handles strict type safety — downgrade to warn for now" to "Enforced — all as-any escapes removed in Phase 8".

    Also consider upgrading these if they now pass cleanly:
    - @typescript-eslint/no-unnecessary-type-assertion
    - @typescript-eslint/require-await

    Run ESLint to confirm zero errors: `npx eslint packages/api/src packages/workers/src packages/shared/src`

    If any lint errors remain after the rule upgrade, fix them in the source files before committing. The pre-commit hook runs lint-staged with --max-warnings=0, so any warnings in staged files will also block commit.
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail && npx eslint packages/api/src packages/workers/src packages/shared/src</automated>
  </verify>
  <done>ESLint exits 0 with no-explicit-any and all unsafe-* rules at error level. The comment in eslint.config.mjs reflects the upgrade.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit -p packages/shared/tsconfig.json && npx tsc --noEmit -p packages/api/tsconfig.json && npx tsc --noEmit -p packages/workers/tsconfig.json` exits 0
2. `npx eslint packages/api/src packages/workers/src packages/shared/src` exits 0
3. `grep -rn 'as any' packages/api/src/ packages/workers/src/ packages/shared/src/` returns no results
4. Existing tests still pass: `cd packages/api && npx vitest run`
</verification>

<success_criteria>
- tsc --noEmit clean across all packages
- ESLint exits 0 with strict type rules at error level
- Zero `as any` casts in backend source code
- All existing Vitest tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-quality-strictness/08-01-SUMMARY.md`
</output>
