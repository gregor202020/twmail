---
phase: 08-code-quality-strictness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routes/webhooks-inbound.ts
  - packages/api/tests/error-shapes.test.ts
autonomous: true
requirements: [QUAL-09]

must_haves:
  truths:
    - "Every API error response conforms to { error: { code, message } } shape"
    - "SNS webhook validation errors return { error: { code, message } } not { error: 'string' }"
  artifacts:
    - path: "packages/api/tests/error-shapes.test.ts"
      provides: "Unit tests asserting error response shape conformance"
      min_lines: 20
  key_links:
    - from: "packages/api/src/routes/webhooks-inbound.ts"
      to: "packages/api/src/plugins/error-handler.ts"
      via: "error shape consistency — inline sends match central handler shape"
      pattern: "error.*code.*message"
---

<objective>
Fix the 4 non-conforming error response sends in webhooks-inbound.ts and add a test verifying the shape.

Purpose: QUAL-09 requires every API error response to use `{ error: { code, message } }`. Four inline `reply.send` calls in the SNS webhook route use `{ error: 'string' }` (bare string instead of object). This plan fixes them and adds a test to prevent regression.

Output: All API error responses conform to the canonical shape. Test file proves it.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-code-quality-strictness/08-RESEARCH.md

<interfaces>
<!-- Canonical error shape from error-handler.ts -->
```typescript
// Central error handler produces:
{ error: { code: string, message: string, details?: Array<{field: string, message: string}> } }
```

<!-- Non-conforming sends in webhooks-inbound.ts (lines ~142, 156, 159, 176): -->
```typescript
// Current (wrong):
return reply.status(403).send({ error: 'Invalid SNS signature' });
return reply.status(400).send({ error: 'Invalid SubscribeURL' });
return reply.status(400).send({ error: 'Invalid SNS Message JSON' });

// Target (correct):
return reply.status(403).send({ error: { code: 'INVALID_SNS_SIGNATURE', message: 'Invalid SNS signature' } });
```

<!-- ErrorCode enum or constants used by AppError (check error-handler.ts or types for existing codes) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix non-conforming error sends and add error shape test</name>
  <files>
    packages/api/src/routes/webhooks-inbound.ts,
    packages/api/tests/error-shapes.test.ts
  </files>
  <action>
    **Step 1 — Fix inline error sends in webhooks-inbound.ts:**

    Change these 4 inline `reply.send` calls to use the canonical shape:

    1. `{ error: 'Invalid SNS signature' }` -> `{ error: { code: 'INVALID_SNS_SIGNATURE', message: 'Invalid SNS signature' } }`
    2. `{ error: 'Invalid SubscribeURL' }` (first occurrence) -> `{ error: { code: 'INVALID_SUBSCRIBE_URL', message: 'Invalid SubscribeURL' } }`
    3. `{ error: 'Invalid SubscribeURL' }` (second occurrence) -> same shape as above
    4. `{ error: 'Invalid SNS Message JSON' }` -> `{ error: { code: 'INVALID_SNS_MESSAGE', message: 'Invalid SNS Message JSON' } }`

    Do NOT restructure to use `throw new AppError` — keep the inline `reply.send` pattern. The SNS caller ignores error bodies; the goal is shape consistency, not control flow refactoring.

    **Step 2 — Create error shape test:**

    Create `packages/api/tests/error-shapes.test.ts` that verifies:
    - Import and inspect the webhooks-inbound route handler or grep the source file
    - Assert that no inline `reply.send({ error: 'string' })` pattern exists in any route file
    - Can use a simple source-code grep approach: read all route files, verify no bare-string error sends exist
    - Alternatively, if the app can be instantiated with `fastify.inject`, test actual error responses from the SNS endpoint with invalid payloads

    The test should cover:
    - SNS request with invalid signature returns 4xx with `{ error: { code, message } }` shape
    - SNS request with invalid JSON returns 4xx with `{ error: { code, message } }` shape

    Use whichever approach is simpler given the existing test setup (vitest.config.ts in packages/api).
  </action>
  <verify>
    <automated>cd C:/Users/User/Dropbox/Claude/twmail/packages/api && npx vitest run tests/error-shapes.test.ts</automated>
  </verify>
  <done>All 4 non-conforming sends fixed. Error shape test passes. No route in the API sends { error: 'string' } — all use { error: { code, message } }.</done>
</task>

</tasks>

<verification>
1. `cd packages/api && npx vitest run tests/error-shapes.test.ts` passes
2. `grep -rn '"error":' packages/api/src/routes/ | grep -v 'code.*message\|AppError\|test'` returns no results (no bare-string error sends)
3. Full test suite still passes: `cd packages/api && npx vitest run`
</verification>

<success_criteria>
- Zero non-conforming error sends in any API route
- Error shape test file exists and passes
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-quality-strictness/08-02-SUMMARY.md`
</output>
